{
  "id": "cost-tracker",
  "name": "Cost and Token Tracker",
  "description": "Monitors token usage and costs, alerting on high usage",
  "trigger": {
    "type": "event",
    "on": [
      "token.usage"
    ],
    "conditions": [
      {
        "operator": "greaterThan",
        "path": "totalCost",
        "value": 0.5
      }
    ]
  },
  "execution": {
    "strategy": "immediate"
  },
  "model": "anthropic/claude-haiku-4-5",
  "output": {
    "format": "jsonl",
    "file": "cost-alerts.jsonl"
  },
  "userPromptText": "High cost detected in codon {{codonId}}:\n\nToken usage: {{inputTokens}} input, {{outputTokens}} output\nCache: {{cacheReadTokens}} read, {{cacheCreationTokens}} created\nTotal cost: ${{totalCost}}\n\nAnalyze if this cost is justified based on the codon's complexity."
}
