{
  "$schema": "https://unpkg.com/hankweave@latest/schemas/hank.schema.json",
  "hank": [
    {
      "id": "codex-web-search",
      "name": "Codex Web Search Test",
      "model": "gpt-5.4",
      "continuationMode": "fresh",
      "promptText": "Use the WebSearch tool to search for 'William Wordsworth poetry' and write a one-sentence summary of the results to a file called search_results.txt."
    }
  ]
}
