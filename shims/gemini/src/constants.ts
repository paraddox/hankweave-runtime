import { STANDARD_TOOLS } from "@shims/common/tools";

export const SHIM_NAME = "gemini-cli-shim";
export const SHIM_VERSION = "0.1.0";

export const KNOWN_TOOLS = [...STANDARD_TOOLS];

export const MAX_SILENT_SUCCESS_RETRIES = 1;
export const MAX_INVALID_JSON_REPAIRS = 2;
